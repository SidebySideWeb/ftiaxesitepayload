# Production Environment Variables for Hetzner Deployment
# Copy this to /var/www/cmsftiaxesite/.env and fill in your values

# Payload Secret (generate with: openssl rand -base64 32)
PAYLOAD_SECRET=your-secret-key-here

# Database Connection
# Format: postgresql://user:password@host:port/database
DATABASE_URI=postgresql://payload:your-password@localhost:5432/cmsftiaxesite

# Node Environment
NODE_ENV=production

# Port (default: 3000)
PORT=3000

# Optional: Custom domain
# NEXT_PUBLIC_SERVER_URL=https://yourdomain.com

# Optional: Media storage (if using S3 or other)
# PAYLOAD_STORAGE_ADAPTER=s3
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret
# AWS_BUCKET=your-bucket
# AWS_REGION=your-region
